---
description: Show the company CLI facade commands
---

Copy and paste the `company` CLI commands you actually use:

```bash
bash .company-kit/scripts/company setup ../my-next-project
bash .company-kit/scripts/company run "로그인 정책 변경 영향도 분석"
bash .company-kit/scripts/company worker backend
bash .company-kit/scripts/company status
bash .company-kit/scripts/company reset
```

Optional hard reset:

```bash
bash .company-kit/scripts/company reset --hard
```
